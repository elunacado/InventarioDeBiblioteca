#include "User.h"

using namespace std;

class Borrower :public User{

private:

public:
	//metodos de la clase Borrower que no recibe ninguna propiedad
	inline Borrower();
	inline Borrower(string _username, string _ID);


};